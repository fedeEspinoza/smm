<div class="page-header">
  <%= link_to prioridads_path, class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Volver
  <% end %>
  <%= link_to edit_prioridad_path(@prioridad), class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-pencil"></span>
    Editar
  <% end %>
  <h4>Ver prioridad</h4>
</div>

<dl class="dl-horizontal">
  <dt>Descripcion:</dt>
  <dd><%= @prioridad.descripcion %></dd>

</dl>
